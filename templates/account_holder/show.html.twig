{% extends 'base.html.twig' %}

{% block title %}AccountHolder{% endblock %}

{% block body %}
    <h1>AccountHolder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ account_holder.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ account_holder.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('account_holder_index') }}">back to list</a>

    <a href="{{ path('account_holder_edit', {'id': account_holder.id}) }}">edit</a>

    {{ include('account_holder/_delete_form.html.twig') }}
{% endblock %}
